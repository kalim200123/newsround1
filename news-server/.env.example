# 이 파일은 .env.example 입니다.
# 이 파일을 복사하여 .env 파일을 생성한 후, 로컬 환경에 맞는 값으로 수정하여 사용하세요.
# Docker Compose 환경에서는 아래 값들이 docker-compose.yml 파일에 의해 덮어쓰여질 수 있습니다.

# 데이터베이스 연결 정보 (로컬에서 직접 실행 시 필요)
DB_HOST=localhost
DB_PORT=3308
DB_DATABASE=news
DB_USER=root
DB_PASSWORD=your_database_password

# 관리자 계정 정보 (초기 생성 시 사용)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password

# JWT 시크릿 키 (아무 랜덤한 문자열로 변경하세요)
ADMIN_JWT_SECRET=replace_this_with_a_real_secret_key
ADMIN_JWT_EXPIRES_IN=12h
